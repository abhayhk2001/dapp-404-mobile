import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const ConfirmPost = () => {
	return (
		<View>
			<Text>ConfirmPost</Text>
		</View>
	);
}

const styles = StyleSheet.create({})

export default ConfirmPost;
